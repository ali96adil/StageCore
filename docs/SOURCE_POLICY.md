# Documentation Source Policy

The Markdown files in `docs/baseline`, `docs/adr`, and `docs/architecture` are the working source of truth for engineering work.

Original Word documents are retained outside the active engineering flow and can be archived separately when needed. This keeps routine edits and AI/Codex reads small, diffable, and token-efficient.
